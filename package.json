{
  "scripts": {
    "build": "npm run clean && npx parcel build src/index.html --no-minify --public-url './' -d ./demo",
    "dev": "npx parcel serve src/index.html -d ./demo",
    "clean": "del demo"
  },
  "dependencies": {
    "@github/webauthn-json": "^0.4.1",
    "parcel": "^2.8.2"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "typescript": "^3.5.3"
  }
}
